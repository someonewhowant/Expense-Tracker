{
  "name": "expense-tracker-nestjs-angular",
  "version": "1.0.0",
  "description": "Expense tracker with NestJS and Angular",
  "scripts": {
    "start": "npm run build --prefix client && npm run build --prefix server && npm run start:prod --prefix server",
    "server": "npm run start:dev --prefix server",
    "client": "npm start --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
